<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skillerapp.skillertutor">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.MainActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".views.more.EditProfileActivity" />
        <activity android:name=".views.more.PrivacySettingsActivity" />
        <activity android:name=".views.courses.CourseNewActivity" />
        <activity android:name=".views.more.TutorFeedbackActivity" />
        <activity android:name=".views.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.more.TutorExperiencesActivity" />
        <activity android:name=".views.more.TutorEducationActivity" />
        <activity android:name=".views.more.TutorEducationNewActivity" />
        <activity android:name=".views.more.TutorExperiencesNewActivity"></activity>
    </application>

</manifest>